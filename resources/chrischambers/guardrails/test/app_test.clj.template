(ns {{top/ns}}.{{main/ns}}-test
  (:require
   [clojure.test :refer [deftest is are testing]]
   [{{top/ns}}.{{main/ns}} :refer :all]))

(deftest {{main/ns}}-test
  (testing "it works"
    (is (= 2 1))))
