;; ---------------------------------------------------------
;; Print current config using command:
;;
;; clojure -M:test/env:test/run --print-config
;; ---------------------------------------------------------

#kaocha/v1
{:tests           [{:id :unit
                    :source-paths ["src"]}]
 :plugins         [:noyoda.plugin/swap-actual-and-expected]
 :reporter        [kaocha.report/documentation]}
